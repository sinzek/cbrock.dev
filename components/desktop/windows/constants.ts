export const DEFAULT_WINDOW_SIZE = { width: 400, height: 300 };
export const MIN_WINDOW_SIZE = { width: 250, height: 150 };
export const MAX_WINDOW_SIZE = {
	width: 10000,
	height: 9000,
}; // arbitarily large since we need to be able to maximize to full screen

export const DEFAULT_WINDOW_SIZE = { width: 600, height: 325 };
export const MIN_WINDOW_SIZE = { width: 340, height: 150 };
export const MAX_WINDOW_SIZE = {
	width: 10000,
	height: 9000,
}; // arbitarily large since we need to be able to maximize to full screen

export const SIDEBAR_MANAGER_WIDTH = 300;
